Frontend (Vite + React) for the NN & RL Lab demo

Run locally:

```bash
cd lab/frontend
npm install
npm run dev
```

This will start the Vite dev server on `http://localhost:5173` by default.
The frontend connects to the backend WebSocket at `ws://localhost:8000/ws`.
